# Desarrollo con el proxy de Vite y producción con reverse proxy same-origin.
VITE_API_URL=/api

# Opcional. Déjalo vacío si el reverse proxy enruta /api/ws al backend.
# Ejemplo de backend separado: wss://api.example.com
VITE_WS_URL=
